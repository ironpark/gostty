// Package fonts discovers system font families and loads the text faces,
// cell metrics, bitmap fallback, and colour emoji used by HyperCat.
// It depends on Ebitengine for font and image types, but owns no terminal,
// tab, window, or settings state. Sizes passed to Load are device pixels.
package fonts
