// Code generated by zigo. DO NOT EDIT.

// Package sys installs the process-global hooks libghostty-vt cannot provide in a library build: a PNG decoder for Kitty graphics, a secure entropy source for Kitty clipboard grants, and a sink for the diagnostics ghostty writes with std.log.
package sys

import "github.com/ironpark/gostty/internal/raw"

// OnPngDecodeRequest: Install the PNG decoder. Until one is installed ghostty refuses PNG
// Kitty transmissions outright; with one, the image is decoded as it
// arrives and reaches `kittyImage` as `rgba`. Process-global, and read
// from whichever thread feeds a stream, so install it at startup.
// Callback callback reentrancy: allowed; it may re-enter the binding while it is running.
// Callback callback thread: caller; it runs on the thread that initiated the native call.
// A panic in a Go callback is rethrown as *CallbackPanicError once the native call returns.
func OnPngDecodeRequest(callback PngDecodeHandler) {
	if callback == nil {
		panic(&CallbackError{Operation: "OnPngDecodeRequest", Callback: "callback", Err: ErrNilCallback})
	}
	callbackHandle := zigoNewPngDecodeHandlerHandle(callback)
	raw.SysOnPngDecodeRequest(uintptr(callbackHandle))
	if zigoCallbackPanicPending() {
		zigoRethrowCallbackPanic("OnPngDecodeRequest", callbackHandle)
	}
}

// ReplyPngImage: Answer the pending PNG request with decoded pixels, four bytes per pixel,
// row-major. `rgba` is copied; it must be exactly `width * height * 4` bytes.
// Native failures are returned as generated error values.
func ReplyPngImage(width uint32, height uint32, rgba []byte) error {
	code := raw.SysReplyPngImage(width, height, rgba)
	if code != 0 {
		return zigoErrorForCode("ReplyPngImage", code)
	}
	return nil
}

// OnSecureRandomRequest: Install the secure entropy source. ghostty uses it for secrets, so it
// must be a real CSPRNG; on failure the operation that needed the bytes
// fails rather than falling back to weaker randomness. Without one,
// ghostty draws from the platform through its own `std.Io`, so this is
// for embedders that want to control the source.
// Callback callback reentrancy: allowed; it may re-enter the binding while it is running.
// Callback callback thread: caller; it runs on the thread that initiated the native call.
// A panic in a Go callback is rethrown as *CallbackPanicError once the native call returns.
func OnSecureRandomRequest(callback SecureRandomHandler) {
	if callback == nil {
		panic(&CallbackError{Operation: "OnSecureRandomRequest", Callback: "callback", Err: ErrNilCallback})
	}
	callbackHandle := zigoNewSecureRandomHandlerHandle(callback)
	raw.SysOnSecureRandomRequest(uintptr(callbackHandle))
	if zigoCallbackPanicPending() {
		zigoRethrowCallbackPanic("OnSecureRandomRequest", callbackHandle)
	}
}

// OnLog: Install the log sink. ghostty writes its internal diagnostics with
// `std.log`, which reads the compile root's `std_options` -- the generated
// shim, which forwards this library's. Without a sink those lines go to
// stderr and an embedder has no way to take them.
//
// Which lines arrive is `std.log`'s own threshold, not this hook's: `.info`
// and above for the release builds, everything in Debug. A parse diagnostic
// -- an unimplemented mode, a malformed Kitty payload -- is `.warn`, so it
// arrives in the archives this repository ships.
//
// Process-global, and called on whichever thread logged, so a handler that
// touches shared state has to do its own locking. The strings are valid for
// the call only.
// Callback callback reentrancy: allowed; it may re-enter the binding while it is running.
// Callback callback thread: caller; it runs on the thread that initiated the native call.
// A panic in a Go callback is rethrown as *CallbackPanicError once the native call returns.
func OnLog(callback LogHandler) {
	if callback == nil {
		panic(&CallbackError{Operation: "OnLog", Callback: "callback", Err: ErrNilCallback})
	}
	callbackHandle := zigoNewLogHandlerHandle(callback)
	raw.SysOnLog(uintptr(callbackHandle))
	if zigoCallbackPanicPending() {
		zigoRethrowCallbackPanic("OnLog", callbackHandle)
	}
}

// Clear: Remove both hooks: PNG transmissions are refused again and entropy
// comes from the platform. Releases the Go callbacks.
func Clear() {
	raw.SysClear()
}

// ReplySecureRandom: Answer the pending request. `bytes` is copied and must be exactly as
// long as the callback's `len`.
// Native failures are returned as generated error values.
func ReplySecureRandom(bytes []byte) error {
	code := raw.SysReplySecureRandom(bytes)
	if code != 0 {
		return zigoErrorForCode("ReplySecureRandom", code)
	}
	return nil
}
